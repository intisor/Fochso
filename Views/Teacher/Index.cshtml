@model List<Fochso.Models.Teacher.TeacherViewModel>

@{
    ViewData["Title"] = "Teachers List";
    int count = 1;
}

@await Html.PartialAsync("_ListAlert", Model)

<div>
    <a asp-controller="Teacher" asp-action="Create">Add New Student</a>
</div>


@if (Model is not null)
{
    <table class="table">
        <thead>
            <tr>
                <th scope="col">S/N</th>
                <th scope="col"> Name</th>
                <th scope="col"> TeachingSubject</th>
                <th scope="col">Action</th>
            </tr>
        </thead>

        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <th scope="row">@count</th>
                    <td scope="row">@item.Name</td>
                    <td scope="row">@item.TeachingSubject</td>
                    <td scope="row">
                        <div class="btn-group">
                            <a asp-controller="Teacher" asp-action="Details" asp-route-id="@item.Id" class="btn btn-primary">View</a>
                        </div>
                    </td>
                </tr>
                count++;
            }
        </tbody>
    </table>
}

